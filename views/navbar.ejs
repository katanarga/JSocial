<div class="ui inverted menu ui top fixed menu">
	<div class="header item" style="color:gold;font-weight: bold;
		font-size: large;">JSocial</div>
		<a class=<%if(page=='profil.ejs'){%>"active item"<%}else{%>"item"<%}%>
			href="<%=linkProfil%>">Mon profil</a>
		<a class=<%if(page=='search.ejs'){%>"active item"<%}else{%>"item"<%}%>
			href="<%=linkSearch%>">Recherche</a>
		<a class=<%if(page=='friends.ejs'){%>"active item"<%}else{%>"item"<%}%>
			href="<%=linkFriends%>">Amis</a>
		<div class="right menu">
		<% if(!isConnected){ %>
		    <a class="ui item" id="linkInscription">S'inscrire</a>
		    <a class="ui item" id="linkConnexion"><i class="user icon"></i>Se connecter</a>
		<% } else { %>
		<div class="ui item ui inline dropdown simple">
		    <div class="text">
		      <img class="ui avatar image" src="/<%=login%>/<%=imgProfil%>">
		      <%=login%>
		    </div>
    		<i class="dropdown icon"></i>
    		<div class="menu">
     			 <a class="item" id="linkDeconnexion" href="http://localhost:8080/deconnexion">
	        		Déconnexion
	        	</a>
     		</div>
     	</div>
	<% } %>
	</div>
</div>
<br><br><br>